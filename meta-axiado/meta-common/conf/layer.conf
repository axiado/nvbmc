# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have recipes-* directories, add to BBFILES
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "axiado-common"
BBFILE_PATTERN_axiado-common = "^${LAYERDIR}/"
BBFILE_PRIORITY_axiado-common = "18"
LAYERSERIES_COMPAT_axiado-common = "nanbield scarthgap whinlatter walnascar"
