# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have recipes-* directories, add to BBFILES
BBFILES += "${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "ax-nvidia-gb300nvl-layer"
BBFILE_PATTERN_ax-nvidia-gb300nvl-layer := "^${LAYERDIR}/"
LAYERSERIES_COMPAT_ax-nvidia-gb300nvl-layer = "whinlatter walnascar"
